# FastAPI backend (server-only — never expose to the browser)
SUPPORT_API_URL=http://localhost:8000
SUPPORT_API_KEY=dev-key

# Support Console login (HttpOnly cookie)
CONSOLE_PASSWORD=console

# Customer portal session HMAC
PORTAL_SESSION_SECRET=dev-portal-secret
